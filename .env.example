# Deployment-only settings. Home Assistant, schedule, and editor settings are
# configured at /admin/ and stored in OUTPUT_DIRECTORY/config.json.
OUTPUT_DIRECTORY=./data
IGNORE_HTTPS_ERRORS=false
PORT=3000
