# Home Assistant Screenshot

Render one or more Home Assistant dashboards in Chromium and expose their latest successful images at stable, unauthenticated URLs for eInk displays and other polling clients.

The administration UI runs behind Home Assistant ingress. Only the public gallery, health endpoints, and image routes are exposed on port 3000.
